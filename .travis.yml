sudo: false

language: go

git:
  depth: 1

go:
  - 1.12.x

install:
  - cd ../..
  - mkdir devchallenge
  - mv alexandear/spy-api devchallenge/spy-api
  - cd devchallenge/spy-api

services:
  docker

before_script:
  - make build
  - make test
  - make docker

notifications:
  email:
    on_success: never
    on_failure: always
